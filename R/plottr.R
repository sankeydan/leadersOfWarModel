plottr = function (is ){
# which ( vars2[,"inf"] == unique ( vars2[,"inf"])[2])
  for ( i in is ){
    #i=43
    title = paste (paste ( names ( vars ) , vars[i,]), collapse = "  ")
    dat = ad[[i]]
    #sq = seq(  1, nrow ( dat) , 1000)
   }
}
